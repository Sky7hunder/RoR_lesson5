<h1>Авторизация</h1>

<%= form_tag('/sessions', method: :post) do%>
  <div class="field">
    <%= text_field_tag :email %>
  </div>
  <div class="field">
    <%= password_field_tag  :password %>
  </div>
  <div class="actions">
    <%= submit_tag 'Войти' %>
  </div>
<% end %>
